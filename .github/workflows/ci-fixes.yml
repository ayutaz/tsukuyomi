# CI/CD Fixes Required
# This file documents the fixes needed to make CI pass

name: CI Fixes Documentation

# Summary of issues found and fixes applied:

# 1. Fixed import issues in src/utils/__init__.py
#    - Added missing imports: normalize_audio, compute_mel_spectrogram, audio_to_mel

# 2. Fixed missing pytest marker
#    - Added 'benchmark' to markers in pyproject.toml

# 3. Created requirements-test.txt with missing dependencies:
#    - PyJWT (for auth tests)
#    - onnx, onnxruntime (for export/optimization tests)
#    - pesq (for evaluation tests)
#    - streamlit (for web UI tests)

# 4. Fixed import in test_data.py
#    - Changed VoiceDataset to TsukuyomiDataset (the actual class name)

# Remaining issues that need to be addressed:

# 1. Missing modules (tests need to be updated or modules created):
#    - src.tools (used in test_compression.py, test_preprocessing.py)
#    - src.models.matcha_tts (used in inference modules)
#    - bigvgan module (not available on PyPI, needs special installation)

# 2. Test implementation issues:
#    - Audio normalization in load_audio multiplies by 0.95 causing test failures
#    - Mel spectrogram tests fail when audio is too short for the FFT size

# 3. Missing implementations referenced in tests:
#    - AudioPreprocessor, TextPreprocessor in src.data.preprocessor
#    - AudioAugmentation, PitchAugmentation, SpeedAugmentation in src.data.augmentation

# To run tests with current fixes:
# pip install -r requirements-test.txt
# pytest tests/test_basic.py tests/test_utils.py -v

# To skip problematic tests:
# pytest -k "not (compression or preprocessing or inference or evaluation or export or auth or web_ui or vocoder or training or streaming or models or integration or performance or data)" -v